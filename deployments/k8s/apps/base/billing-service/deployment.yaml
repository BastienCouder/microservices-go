apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  labels:
    app: billing-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
    spec:
      serviceAccountName: billing-service
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: app-secrets
          secret:
            secretName: app-secrets
      containers:
        - name: billing-service
          image: microservices-go/billing-service:prod
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
              name: http
          env:
            - name: INTERNAL_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: internal_jwt_secret
          envFrom:
            - configMapRef:
                name: billing-service-config
          volumeMounts:
            - name: app-secrets
              mountPath: /run/secrets
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["/app/service", "healthcheck"]
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["/app/service", "healthcheck"]
            initialDelaySeconds: 30
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
