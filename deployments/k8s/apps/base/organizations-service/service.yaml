apiVersion: v1
kind: Service
metadata:
  name: organizations-service
  labels:
    app: organizations-service
spec:
  type: ClusterIP
  selector:
    app: organizations-service
  ports:
    - name: http
      port: 8084
      targetPort: http
